"use client";

export default function LoadingSpinner() {
  return (
    <div className="inline-block animate-spin rounded-full h-8 w-8 border-t-2 border-b-2 border-accent" aria-label="Yükleniyor"></div>
  );
} 